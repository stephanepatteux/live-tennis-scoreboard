{
  "name": "Tennis Trader Board",
  "install": "bash scripts/cloud-install.sh",
  "terminals": [
    {
      "name": "web",
      "command": "source .venv/bin/activate && HOST=0.0.0.0 PORT=5000 python wsgi.py",
      "description": "Tennis Trader Board dev server on http://localhost:5000 (demo mode unless LIVE_TENNIS_API_KEY is set)"
    }
  ],
  "ports": [
    {
      "name": "web",
      "port": 5000
    }
  ]
}
